/** Tinggi viewer. Dipakai oleh slot (pembungkus) agar layout tidak bergeser saat viewer dimuat. */
export const VIEWER_SIZE = 'h-[440px] sm:h-[520px] lg:h-[620px]';
/** Kulit viewer (dipasang absolute inset-0 di dalam slot). */
export const VIEWER_SHELL =
  'overflow-hidden rounded-3xl border border-slate-200 bg-studio shadow-[0_24px_60px_-28px_rgba(14,27,48,0.35)]';
